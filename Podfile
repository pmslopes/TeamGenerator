# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TeamGenerator' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  
  # Pods for TeamGenerator 
  pod 'Firebase/Core'
  pod 'Firebase/Database'

  target 'TeamGeneratorTests' do
    # Pods for testing
  end

  target 'TeamGeneratorUITests' do
    # Pods for testing
  end

end
